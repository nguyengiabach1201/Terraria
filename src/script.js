const worldSize = 64;

function generateTerrain(frequency, seed) {
  for (let x = 0; x < worldSize; x++) {
    for (let y = 0; y < worldSize; y++) {
      let v = noise();
    }
  }
}

console.log(noise(1, 2));